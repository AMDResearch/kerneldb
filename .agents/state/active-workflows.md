# Active Workflows

`amplify` generates this file for the target repository. It is the project-wide coordination index for concurrent workflow execution.

Packet directories follow the pattern: `.agents/workflows/<state>/<workflow-id>/`
